# Replace page placeholders with supplied content

## Scope
- Add the supplied legal copy to `/criteria`, `/innovation`, `/viability`, `/scalability`, and the three research articles.
- Preserve the document wording, including its fee references as approved.
- Keep the two explicitly marked Chris Dias placeholders in the research articles.
- Keep the existing assessment questions, scoring, thresholds, navigation, calls to action, and disclaimers unchanged.

## Implementation
- Store the approved page copy as structured project content rather than displaying the uploaded editorial notes.
- Render headings, paragraphs, emphasis, lists, and quotations accessibly within the existing page design.
- Retain the criteria-page links, assessment prompts, article bylines, contents lists, and professional-help blocks around the new copy.
- Update page summaries only where needed to reflect that the pages now contain substantive content.

## Verification
- Check that no generic content placeholders remain except the two deliberately retained passages.
- Verify all seven affected routes render their own supplied text, metadata, and links.
- Confirm the project builds and the prerendered route HTML contains the new copy.
